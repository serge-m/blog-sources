language: python
branches:
  only:
  - master
install:
- pip install pelican
script:
- $(npm bin)/set-up-ssh --key "$encrypted_XXXXXXXXXXXX_key" --iv "$encrypted_XXXXXXXXXXXX_iv" --path-encrypted-key ".travis/github_deploy_key.enc"
- make publish
- make github
env:
  global:
